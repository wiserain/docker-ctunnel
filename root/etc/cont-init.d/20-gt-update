#!/usr/bin/with-contenv bash

if [ "$(printenv USE_GT | tr -d '"' | tr -d "'")" == "true" ]; then

    # update green-tunnel silently
    npm i -g green-tunnel &> /dev/null
    echo "[cont-init.d] green-tunnel v$(gt --version) ready"

fi
